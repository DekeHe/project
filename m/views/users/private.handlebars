<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>private</title>
</head>
<body>
    
    <p>username is:{{user.username}}</p>
    <p>password is:{{user.password}}</p>
    <p>email is:{{user.email}}</p>

    <p>gender is:{{user.gender}}</p>
    <a href="/users/logout">log out here</a>
</body>
</html>